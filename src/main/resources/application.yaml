spring:
  application:
    name: flight-track

server:
  port: 8080

opensky:
  base-url: ${OPENSKY_BASE_URL:https://opensky-network.org}
  username: ${OPENSKY_USERNAME:}
  password: ${OPENSKY_PASSWORD:}
